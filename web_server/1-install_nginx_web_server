#!/usr/bin/env bash
# Install and configure Nginx web server

# Update package list
apt-get update -y

# Install nginx
apt-get install nginx -y

# Replace default page with required string
echo "Holberton School" > /var/www/html/index.nginx-debian.html

# Start nginx service (without systemctl)
service nginx start
